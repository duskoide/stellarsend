// Ambient Worker types. @cloudflare/workers-types provides Queue, MessageBatch,
// ScheduledEvent, etc. globally once included in tsconfig `types`. This file is a
// placeholder for any project-specific global augmentations.

export {};
